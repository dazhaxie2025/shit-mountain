# ⚠️ WARNING — 阅读前请三思

> **本仓库仅用于行为艺术与反模式教学。**
> 它存在的意义，是让你看清楚"代码能烂到什么程度"，而不是让你运行它。

## 你正在面对什么

本项目（🗻 Shit Mountain）的源码包含但不限于以下危险行为：

- **自修改源码**：运行时会往自身源文件追加内容，并在你的文件系统上留下痕迹。
- **子进程复制**：运行时会用当前 Python 解释器启动自己的副本，形成进程链。
- **危险反序列化**：对用户输入直接执行 `pickle.loads` / `yaml.load` / `eval` / `exec`，可导致任意代码执行。
- **敏感信息泄露**：硬编码的密码、token、密钥，以及**完整的环境变量**会被写入日志。
- **系统资源滥用**：关闭垃圾回收、无意义线程、自引用缓存、锁交叉嵌套、忙等待与哈希燃烧。
- **不可预测行为**：退出码随机、结果由"掷筊 / 抛硬币 / 黄历"决定，输出不可复现。

## 免责声明

- 作者**不对任何运行后果负责**，包括但不限于：数据丢失、环境变量泄露、磁盘被写满、
  源码被自我修改、进程失控、精神创伤、以及与佛祖失联。
- 本仓库**不提供任何形式的保证**，连"能跑"都不保证（虽然它大部分时候能跑）。
- 任何形式的商业用途、生产部署、教学演示，风险自负。

## 建议

- **仅在隔离环境查看源码**，例如一次性 Docker 容器、虚拟机快照、或一台即将格式化的旧电脑。
- **不要执行**。如果实在按捺不住，请先读完 `README.md` 的警告清单，并默念三遍：

> 沐浴更衣、面向东方、心中默念三遍。

---

> 佛祖保佑，永无 BUG。
